<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/styles/login.css">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
  

</head>
<body>
<div class="logo">
        <img src="/images/4shortwhite.png" class="logo" alt="4SHORT" srcset="">
    </div>
            <div class="background">
                <div class="shape"></div>
                <div class="shape"></div>
            </div>
                <form action="/login" method="POST">
                        <h2>Login</h2>
          {{#if message }}
    <h4 class="alert alert-danger mt-4">{{message}}</h4>
{{/if}}
                        <div class="form-group">
                            <label for="loginEmail">Email</label>
                            <input type="text" placeholder="Email Address" class="form-control" id="loginEmail" name="email" required>
                        </div>
                        <div class="form-group">
                            <label for="loginPassword">Password</label>
                            <input type="password" placeholder="............" class="form-control" id="loginPassword" name="password" required>
                        </div>
                        
                        <button type="submit" value="Login" class="btn btn-success">Login</button>
                    
                        <a href="/404" class="forgot btn btn-danger" onclick="textsuccess">Forgot your password?</a>
                     <hr class="hr">
                        <p class="gotta">Don't have an account? We gotta you.</p>
                       <a href="/signup" class="sign">Sign up</a>
                     
                    </div>
            
                        </form>
                         <script>
       function displayErrorMessage(message) {
    const errorMessageElement = document.getElementById('error-message');
    if (errorMessageElement) {
        errorMessageElement.textContent = message;
        errorMessageElement.style.display = 'block'; // Ensure it's visible
    }
}

// Assume there's a function to clear the error message
function clearErrorMessage() {
    const errorMessageElement = document.getElementById('error-message');
    if (errorMessageElement) {
        errorMessageElement.textContent = ''; // Clear the message
        errorMessageElement.style.display = 'none'; // Hide the element
    }
}

// Example usage in an event handler or function
// Display error message
displayErrorMessage("User not found");

// Clear error message
clearErrorMessage();

    </script>
                        </div>
                </div>
                    </div>
               

 
   
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.min.js"></script>
   {{!-- <script src="../public/error.js"></script> --}}
</body>
</html>
